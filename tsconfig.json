{
  "compilerOptions": {
    "target": "es5",                          // ES5로 트랜스파일 (브라우저 호환성 높임)
    "lib": ["dom", "dom.iterable", "esnext"], // 사용하는 라이브러리 정의
    "allowJs": true,                          // JS 파일도 허용 (JS/TS 혼용 가능)
    "skipLibCheck": true,                     // 타입 정의 파일 검사 생략
    "strict": false,                          // 엄격 모드 해제 (처음엔 false로 두는 게 편함)
    "forceConsistentCasingInFileNames": true, // 파일명 대소문자 일치 강제
    "noEmit": true,                           // 실제 JS 파일은 출력하지 않음
    "esModuleInterop": true,                  // CommonJS 모듈 호환
    "module": "esnext",                       // 최신 모듈 시스템 사용
    "moduleResolution": "node",               // Node 방식 모듈 해석
    "resolveJsonModule": true,                // JSON import 허용
    "isolatedModules": true,                  // 각 파일을 개별 모듈로 처리
    "jsx": "preserve"                         // JSX 문법 그대로 유지 (Next.js에서 처리)
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
